## Friction: 2026-03-23 PR #26

**Category:** Tooling/Environment
**Severity:** Medium (cost ~5 min, 2 round trips)

**Issue:** 
1. No Rust toolchain in current environment - had to install rustup mid-session
2. Clippy doc-markdown errors not caught before push
3. Format commit amended after first push (force push required)

**Root cause:** 
- Pre-push validation gap - working in session without cargo available
- CI has stricter clippy rules than I assumed

**Fix:**
- Installed rustup: `curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh`
- Fixed all doc markdown (QName → `QName`, etc.)
- Converted manual Default impl to derive
- Converted match to let...else

**Pattern to add:**
Every PR branch should start with:
```bash
#!/bin/bash
set -e
cargo fmt --all
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo xtask alpha-check
echo "✅ Pre-push checks passed"
```

**Action:** Create `scripts/pre-push.sh` after #26 merges.
